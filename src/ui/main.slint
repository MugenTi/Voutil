// main.slint
import { AppWindow } from "./appwindow.slint";
import { SettingsWindow } from "./settings_window.slint";
import { ResizeWindow } from "./resize_window.slint";

// Export all components used on the Rust side
export { AppWindow, SettingsWindow, ResizeWindow }
//export { AppWindow, SettingsWindow }